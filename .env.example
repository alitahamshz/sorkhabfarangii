# آدرس مستقیم بک‌اند؛ فقط روی سرور Next.js قابل دسترسی است.
API_BASE_URL=https://sorkhabfarangi.shop/api/v1

# اختیاری: فقط برای API عمومی که اجازه فراخوانی مستقیم از مرورگر را دارد.
# در معماری BFF و احراز هویت، مقدار پیش‌فرض /api استفاده می‌شود.
# NEXT_PUBLIC_API_BASE_URL=https://example.com/api/v1
